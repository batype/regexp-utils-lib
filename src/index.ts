import reg from './regex';
import { is_valid_ipv4, is_valid_ipv4_mask } from './valid/is_valid_ipv4';
import { is_valid_ipv6, is_valid_ipv6_mask } from './valid/is_valid_ipv6';

export {
  is_valid_ipv4,
  is_valid_ipv4_mask,
  is_valid_ipv6,
  is_valid_ipv6_mask,
  reg,
};
